services:
  - type: web
    name: pokemon-board
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    disks:
      - name: data
        mountPath: /data
        sizeGB: 1
    envVars:
      - key: NODE_VERSION
        value: "18"
